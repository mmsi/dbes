dbes TODO list:

-Make zeroing non-volatile. save to configuration file every time the jack is
	zeroed. As-is the system must be zeroed every time it is setup or power is
	lost

-Graceful recovery from a CAN busoff condition
	slave and master

-Add jack database functionality

-Status requesting
	elevation

-Streamline calibration
